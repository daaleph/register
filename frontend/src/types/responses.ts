export interface BFIResponse {
    profileId: string;
    variable: string;
    answer: number[];
}

export interface ProfileResponse {
    profileId: string;
    variable: string;
    answer: number[];
}

export interface ProfileOtherResponse {
    profileId: string;
    variable: string;
    answer: string;
}

export interface ProductResponse {
    profileId: string;
    variable: string;
    answer: number[];
}

export interface ProductOtherResponse {
    profileId: string;
    variable: string;
    answer: string;
}